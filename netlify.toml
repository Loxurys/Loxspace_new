[build]
  command = "npm run build:netlify"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[dev]
  framework = "#custom"
  command = "node scripts/start-netlify-server.js"
  targetPort = 4174
  port = 8888
  publish = "."
  autoLaunch = false
